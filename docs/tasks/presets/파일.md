# 자주 쓰는 일 — 파일

| 파일 | 하는 일 |
|---|---|
| `src/screens/day/PresetChips.tsx` | 칩 줄. 누르면 그날 목록에 추가 |
| `src/sheets/PresetListSheet.tsx` | 설정 안 목록. 수정 · 삭제 · 새로 추가 |
| `src/sheets/PresetSheet.tsx` | 편집. 날짜 칸만 빼고 할 일 폼을 그대로 쓴다 |
| `src/sheets/TaskFormFields.tsx` | 공유하는 폼 (`showDate={false}`로 날짜를 감춘다) |
| `src/lib/store.tsx` | `addPreset` `updatePreset` `removePreset` `applyPreset(id, date)` |
| `src/lib/selectors.ts` | `presetsFor(presets, filter)` |
| `src/lib/types.ts` | `Preset` |
| `src/components/ScrollRow.tsx` | 칩이 넘칠 때 끝을 흐린다 |

## 조심할 것

- **비어 있다고 채워 넣지 않는다.** 전체 초기화한 상태와 구분할 방법이 없다
- `TaskSheet`의 `자주 쓰는 일로 저장` 체크박스도 여기로 들어온다.
  같은 제목이 있으면 안 만든다
