# 세 화면 — 파일

| 파일 | 하는 일 |
|---|---|
| `src/screens/DayScreen.tsx` | 일별 화면 짜맞추기 |
| `src/screens/day/TaskRow.tsx` | ★ 할 일 한 줄. **메모를 그리는 유일한 곳** |
| `src/screens/day/ProgressStrip.tsx` | 진행 스트립 (`세 가지 중에 한 가지 했어요`) |
| `src/screens/day/StaleSection.tsx` | 지난 미완료. 접힌 채로, 오늘 화면에서만 |
| `src/screens/MonthScreen.tsx` | 달력 + 위 세 숫자 |
| `src/screens/month/MonthCell.tsx` | 달력 한 칸. 제목 3개까지, 넘치면 `+N` |
| `src/screens/LogScreen.tsx` | 주간·월간 세그먼트 + 완료율 |
| `src/screens/log/CategoryBars.tsx` | 카테고리별 완료 막대 (**월간만**) |
| `src/screens/log/HabitGrid.tsx` | 격자. 주간·월간을 같이 그린다 |
| `src/lib/selectors.ts` | 날짜·월·기간 조회, 격자 행 만들기 (`habitRows` `tasksInRange`) |
| `src/lib/date.ts` | `weekStartOf` `daysFrom` |
| `src/lib/ui.tsx` | `logSpan` — 주간인지 월간인지 |
| `src/sheets/TaskSheet.tsx` | 추가·수정 시트 |
| `src/sheets/TaskFormFields.tsx` | 폼 부품 (즐겨찾기 시트와 공유) |
| `src/components/TabBar.tsx` | 하단 `일 / 월 / 기록` |

## 조심할 것

- **격자의 `grid`는 `minmax(0,1fr)`로 쓴다.** 그냥 `1fr`이면 칸이 안 줄어들어 화면 밖으로 밀린다
- **`aspect-square`와 `min-height`를 같이 주지 않는다.** 최소 높이가 최소 너비로 번져서
  31칸이 화면보다 넓어진다. 한 번 겪었다
- **주간은 `minmax(0,34px)`로 칸을 묶는다.** `1fr`로 두면 일곱 칸이 폭을 나눠 가져
  한 칸이 60px이 된다
- **`habitRows`의 `mark`는 날짜 문자열이 열쇠다.** 1~31 숫자로 두면 주가 달을
  넘어갈 때(8/31~9/6) 두 달 것이 같은 칸에 겹친다
- 메모를 다른 화면에 그리고 싶어지면 먼저 [기능.md](기능.md)를 읽는다. 일부러 뺀 것이다
