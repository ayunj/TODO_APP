# 메모 — 파일

| 파일 | 하는 일 |
|---|---|
| `src/screens/MemoScreen.tsx` | 목록. 방 칩 줄 · 새 메모 · 찾기 · 어느 장을 펼칠지 |
| `src/screens/memo/MemoCard.tsx` | 한 장. 접힘(두 줄 + 날짜 + 방 칩) / 펼침(자라는 입력 칸) |
| `src/sheets/MemoRoomsSheet.tsx` | 같이 보기 — 이 메모를 어느 방들에 둘까 |
| `src/components/ScopeChips.tsx` | 맨 위 방 칩 줄 (`전체`·`나만`·방). 장보기와 같이 쓴다 |
| `src/lib/store.tsx` | `memos` `addMemo` `updateMemo` `setMemoRooms` `removeMemo` `markMemosSeen` |
| `src/lib/types.ts` | `Memo`(제목 칸 없음, `roomIds` `updatedBy`), `Settings.memoSeenAt` |
| `sql/2026-08-10_메모-여러-방.sql` | `room_ids` `updated_by` · 메모만 쓰는 RLS · 방 닫을 때 떼어내기 |
| `src/components/Header.tsx` | 메모 아이콘 + 안 본 표시 점 |
| `src/components/Icons.tsx` | `MemoIcon` `SearchIcon` |
| `src/lib/repo/indexeddb.ts` | `memo` 스토어 |
| `src/lib/repo/remote.ts` | `memos` 테이블 변환 |

## 저장 방식

화면 → 목록 상태 → 디스크 순으로 간다.

글자 하나마다 디스크에 쓰지는 않지만(600ms 쉬면 쓴다) **목록 상태는 늘 최신**이다.
그래서 다 적기 전에 바로 `새 메모`를 눌러도 방금 쓴 게 안 사라진다.

지울 때는 **대기 중인 저장도 같이 취소**한다. 안 그러면 지운 메모가 되살아난다.

## 안 본 표시

```
안 본 것 있음 = memos.some(m => m.text.trim() && m.updatedAt > memoSeenAt)
```

`memoSeenAt`은 메모 화면에 **들어올 때와 나갈 때** 찍는다.

## 조심할 것

- 카드를 `key={memo.id}`로 그린다. 다른 장을 펼칠 때 입력 상태가 섞이지 않게
- 펼친 카드의 테두리도 `outline-offset: -2px` (바깥으로 그리면 잘린다)
- 한 장짜리로 쓰던 시절 데이터에는 `createdAt`이 없다. 불러올 때 `updatedAt`으로 채운다
- 방 하나만 걸리던 시절 줄은 `room_id` 하나를 들고 있다.
  **불러올 때 `[room_id]`로 읽어 옮긴다** — 서버에도 한 번 옮겨 담는 SQL이 있다
- 점 판단에 쓰는 `memoSeenAt`은 **화면에 들어온 순간의 값을 붙잡아 둔다.**
  들어오면서 바로 찍기 때문에, 그대로 쓰면 점이 뜰 새가 없다
- 메모는 `room_id`로 못 거른다. `pull()`에서 메모만 그 조건을 빼고 **RLS에 맡긴다**
