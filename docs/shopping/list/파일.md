# 장보기 목록 — 파일

| 파일 | 하는 일 |
|---|---|
| `src/screens/ShopScreen.tsx` | 화면 짜맞추기. 오늘 담은 것까지만 목록에 남긴다 |
| `src/screens/shop/ShopAddRow.tsx` | 늘 떠 있는 입력 줄. 엔터로 이어 적기 |
| `src/screens/shop/ShopRow.tsx` | 한 줄. 체크 · 이름 · 메모 · 구입처 배지 · 담은 날 |
| `src/sheets/ShopItemSheet.tsx` | 이름 / 어디서 / 메모. 구입처 자동완성 |
| `src/lib/selectors.ts` | ★ `onShopList(item, today)` — 목록과 기록을 가르는 한 줄 |
| `src/lib/store.tsx` | `addShopItem` `updateShopItem` `toggleShopItem` `removeShopItem` |
| `src/lib/types.ts` | `ShopItem` |
| `src/components/Header.tsx` | 장바구니 아이콘 + 개수 배지, 밀고 들어간 화면의 뒤로가기 |
| `src/lib/ui.tsx` | `pushView('shop')` / `popView()` |
| `src/components/AppShell.tsx` | 이 화면에서는 탭바·FAB를 내린다 |

## 가르는 규칙

```ts
onShopList(item, today) = !item.done || item.boughtOn === today
```

한 줄이 전부다. 화면과 스토어가 같은 걸 쓴다.

## 조심할 것

- **날짜·주기·카테고리를 넣지 않는다.** 넣고 싶어지면 [기능.md](기능.md)를 먼저 읽는다
- 입력 칸 포커스 테두리는 `outline-offset: -2px`.
  바깥으로 그리면 고정 헤더에 덮여 윗변이 잘린다 (한 번 겪었다)
- `roomId` `doneBy` 칸은 뚫어놨지만 아직 늘 `null`이다 (방 붙일 때 쓴다)
