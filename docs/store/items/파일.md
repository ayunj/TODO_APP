# 상점 — 파일

| 파일 | 하는 일 |
|---|---|
| `supabase/schema.sql` | ★ `costume_catalog` · `costume_owned` · `gomdori` · `buy_costume()` · `grant_poses()` |
| `sql/2026-08-25_곰돌이-포인트-상점.sql` | 이미 도는 DB에 얹는 같은 것 |
| `src/lib/costumes.ts` | ★ **파는 것 목록** — `DAILY` · `COSTUMES` · `BEARS` · `ROOMS` · `SETS` · `costumeOf()` |
| `src/lib/types.ts` | `Costume` · `CostumeSet` · `Gomdori` |
| `src/lib/gomdori.tsx` | ★ `GomdoriProvider` — 잔액·가진 것·입은 것을 들고 `buy` · `wear` |
| `src/lib/repo/remote.ts` | `pullGomdori()` · `buyCostume()` · `wearCostume()` |
| `src/screens/StoreScreen.tsx` | ★ 상점 화면 — 제목 줄, **칩 여섯**(`all` `daily` `costume` `season` `room` `mine`), 격자 |
| `src/screens/store/StoreCard.tsx` | 격자 칸 하나 |
| `src/screens/store/SetDetail.tsx` | 시즌 세트 펼친 것 |
| `src/screens/store/Stage.tsx` | 곰돌이가 서는 자리 — 방 위에 곰 |
| `src/screens/store/Coin.tsx` | 동전 표 |
| `src/screens/HomeScreen.tsx` | 상점으로 가는 **띠 하나.** 로그인해야 뜬다 |
| `src/screens/home/RoomCard.tsx` | 홈의 곰돌이와 방 |
| `src/lib/ui.tsx` | `{ kind: 'store' }` — 밀고 들어가는 화면 |
| `src/app/providers.tsx` | `GomdoriProvider`를 얹는 자리 |
| `public/gomdori/` | 그림 — 앱과 같이 나간다 |
| `design/상점.html` | 시안 |

## 값이 흐르는 길

```
홈 상점 띠 → pushView({kind:'store'}) → StoreScreen

화면 열림 → pullGomdori()
              ├ my_points()                     잔액
              ├ costume_owned.select            가진 것
              └ gomdori.select                  입은 것

카드 누름 → 걸쳐보기(trying)          ← 아직 안 산다. Stage에 그대로 세워본다
사기 누름 → buy_costume(key)
              ├ 값표에서 값을 찾는다   ← 앱이 보낸 값은 안 쓴다
              ├ 포즈·숨김·잔액을 막는다
              ├ costume_owned에 한 줄 (산 값을 박아서)
              └ grant_poses()          세트가 찼으면 포즈가 딸려 들어온다
          → 남은 포인트
          → refresh() → wearCostume()  ← 산 것은 바로 입힌다
```

## 조심할 것

- **값은 앱이 안 보낸다.** `buy_costume(item)`은 열쇠 하나만 받는다 —
  값을 같이 받으면 `이 옷 0원이요`를 막을 수가 없다
- **`costume_owned`에 insert 정책이 없다.** 넣는 길은 `buy_costume` 하나다.
  열면 잔액을 안 보고 그냥 넣을 수 있다
- **산 값을 박아둔다.** 값이 바뀌어도 이미 산 것은 그때 값이라야 잔액이 안 흔들린다
- **포즈는 못 산다.** 파는 물건이 아니라 세트를 다 모으면 오는 것이다
- **세트는 살 때와 잔액을 볼 때 둘 다 확인한다.** 살 때만 보면
  [숨겨뒀던 소품](../admin/기능.md)이 켜졌을 때 뭔가 하나 더 사기 전까지 못 받는다
- **`costumeOf()`는 없는 열쇠면 기본 곰돌이를 준다.** 서버에만 있고 앱에는 없는 옷이
  내려와도 화면이 비지 않는다
- **목록이 둘이다.** 값을 고칠 때는 `costumes.ts`와 `costume_catalog`를 **같이** 고친다.
  어긋나면 서버가 맞다
- **그림이 없는 것**(`img` 없음)은 아직 안 그린 것이다. 파일만 넣으면 뜬다
  ([그릴 것](../../../design/g/그릴-것.md))
- **갈래 이름이 양쪽에서 다르다.** 앱은 `group`, 서버는 `family`. 값은 같다
- **`season`이 있는 줄에는 갈래를 안 단다.** 달면 시즌 칩과 갈래 칩에 두 번 뜬다.
  서버의 `family` 채우기가 `season is null`을 보는 까닭이다
- **`BEARS`는 `DAILY`와 `COSTUMES`를 이어 붙인 것이다.** 곰 옷을 한 줄로 볼 때만 쓴다 —
  칩은 갈래별 목록을 따로 본다
