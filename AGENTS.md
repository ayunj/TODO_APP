# 이 코드를 고칠 때

AI 코딩 도구와 새로 합류한 사람이 먼저 읽는 문서.
[docs/기능.md](docs/기능.md)는 무엇을 왜 만들었는지, [docs/파일.md](docs/파일.md)는 어디를 고쳐야 하는지 알려준다.

---

## 이 앱이 무엇인가

**반복되는 집안일을 나눠서 놓치지 않게 하는 앱이다. 범용 할 일 관리 앱이 아니다.**

이 한 줄이 대부분의 판단을 대신해준다. 태그·정렬 옵션·하위 작업 같은 걸 넣자는 생각이 들면
"8일마다 화장실 청소"라는 장면에 그게 필요한지 먼저 물어야 한다.

---

## 손대기 전에 알아야 할 규칙

### 1. 반복 주기가 핵심이다

`src/lib/repeat.ts`를 고칠 일이 생기면 `repeat.test.ts`를 먼저 읽는다.

- **미래 회차를 미리 여러 개 만들지 않는다.** 완료할 때 다음 1건만
- 다음 회차 기준일은 **실제로 체크한 날과 그 할 일의 날짜 중 늦은 쪽**이다.
  완료일만 쓰면 미리 체크했을 때 같은 날 두 건이 생긴다 — 실제로 터졌던 버그다
- **삭제**는 그 한 건만 지운다. 다음 회차는 남는다
- **완료 취소**는 그 항목이 낳은 미완료 회차를 거둔다. 삭제와 뜻이 다르다

### 2. 층을 건너뛰지 않는다

```
화면 → useStore()/useUi()/useAuth() → Repository 인터페이스 → 로컬 + 서버
```

- 화면에서 `supabase()`나 IndexedDB를 직접 부르지 않는다
- 스토어는 Supabase를 모른다. `Repository`만 안다
- 새 데이터 종류를 더할 때는 `types.ts` → `repository.ts` → `repo/indexeddb.ts` →
  `repo/memory.ts` → `repo/remote.ts` → `repo/synced.ts` → `store.tsx` 순서로 지난다

### 3. 화면은 서버를 기다리지 않는다

모든 쓰기는 낙관적이다. 화면을 먼저 바꾸고 저장은 뒤에서 간다.
`await`을 걸어 사용자를 기다리게 만들지 않는다. 실패하면 토스트로 알리고 화면은 그대로 둔다.

### 4. 지우기는 흔적을 남긴다

서버는 `deleted_at`, 로컬은 무덤(`graveyard`). 진짜로 지우면 다음 동기화에 되살아난다.
`delete` 정책이 SQL에 일부러 없다.

### 5. 색은 한 곳에서만

`src/app/globals.css`의 `:root` 블록과 `src/lib/constants.ts`의 팔레트.
컴포넌트에 `#RRGGBB`를 직접 적지 않는다 — 토큰이 없으면 새로 만든다.

`--accent`는 앱 강조색으로 **고정**이다. 카테고리를 눌러도 안 바뀐다.

### 6. 날짜는 문자열이다

앱 전체에서 `'YYYY-MM-DD'`. `Date` 객체는 `src/lib/date.ts` 안에서만 산다.

---

## 바꾸지 말 것 (묻지 않고는)

프로토타입을 쓰면서 정한 것들이다. 좋은 뜻으로 되돌려 놓기 쉬운 것들이라 적어둔다.

1. **밀린 일을 빨갛게 칠하지 않는다.** 지났으면 `3일 지남`이라고만 적는다
2. **완료한 항목을 숨기지 않는다.** 아래로 내리고 취소선만 긋는다
3. **주기는 사람을 다그치는 도구가 아니다.** 못 지켰다고 경고하지 않는다
4. **월별은 무엇을 하는 날인지 보여준다.** 막대 그래프로 바꾸지 않는다
5. **카테고리는 하나만 고른다.** 태그를 만들지 않는다
6. **장보기는 할 일이 아니다.** 날짜·주기·우선순위·카테고리가 없다.
   날짜를 붙이면 안 산 우유가 `3일 지남`으로 뜬다
7. **메모는 대화창이 아니다.** 흘러가지 않는 종이 한 장이다
8. **자동 이월을 넣지 않는다.** 항목이 저절로 옮겨 다니면 기록 격자의 지난 칸이 빈다
9. **메모는 일별 화면에서만 보인다.** `screens/day/TaskRow.tsx`만 메모를 그린다

---

## 자주 밟는 지뢰

- **Tailwind 설정을 고쳤으면 `.next`를 지우고 다시 띄운다.** 서버만 재시작하면
  이전 CSS가 그대로 나온다. 새 색 클래스가 통째로 빠져서 한참 헤맸다
- **`aspect-square`에 `min-height`를 같이 주지 않는다.** 최소 너비로 번져서
  좁은 화면에서 격자가 밖으로 밀려난다. `grid`는 `minmax(0,1fr)`로 쓴다
- **입력 칸 포커스 테두리는 `outline-offset: -2px`.** 바깥으로 그리면 고정 헤더에 덮여 잘린다
- **버튼에 `text-align: inherit`를 주지 않는다.** 글자가 죄다 왼쪽으로 붙는다
- **id는 uuid로 만든다.** 서버 컬럼이 uuid라 다른 모양은 거부당한다
- **PowerShell에서 커밋 메시지에 큰따옴표를 쓰지 않는다.** 인자가 쪼개진다.
  긴 메시지는 파일로 만들어 `git commit -F`

---

## 작업 순서

```bash
npx tsc --noEmit     # 타입
npm test             # 반복 주기 규칙
npm run build        # 빌드
git push origin main # Vercel이 자동 배포
```

**작업이 끝나면 묻지 말고 배포한다.** 사용자가 폰에 설치해 직접 쓰는 앱이라
배포되지 않으면 확인 자체가 불가능하다.

단, **저장된 데이터를 지우거나 되돌리기 어려운 변경은 먼저 묻는다.**
배포되고 나면 폰에서 이미 지워진 뒤라 손쓸 방법이 없다.

배포 확인은 JS 청크에서 바뀐 문구를 찾는다 — 앱이 클라이언트 렌더라
HTML만 봐서는 반영 여부를 알 수 없다.

---

## 말투

화면에 나가는 글, 주석, 커밋 메시지 모두 한국어다.

- **명령하지 않고 거들어준다.** `추가하세요` 보다 `적어두세요`
- 숫자를 세는 대신 말로 — `세 가지 중에 한 가지 했어요`
- 주석은 *무엇*이 아니라 *왜*를 적는다.
  코드를 보면 아는 것은 쓰지 않고, 보면 모르는 것만 남긴다
